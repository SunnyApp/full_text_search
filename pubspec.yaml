name: full_text_search
version: 0.7.5+1
homepage: https://github.com/SunnyApp/full_text_search
description: Full-text search that takes advantages of isolates to prevent locking the main thread
environment: 
  sdk: '>=2.12.0 <3.0.0'
dependencies: 
  collection: ^1.15.0
  equatable: ^2.0.0
  logging: ^1.0.0
  meta: ^1.3.0
  sunny_dart: ^0.8.0+7
  flutter: 
    sdk: flutter
dev_dependencies: 
  flutter_test: 
    sdk: flutter

flutter: null
